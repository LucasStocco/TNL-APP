import 'package:flutter/material.dart';

class RelatorioItemScreen extends StatelessWidget {
  const RelatorioItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Relatório de compras em desenvolvimento',
        style: TextStyle(
          fontSize: 16,
          color: Color(0xFF777777),
        ),
      ),
    );
  }
}